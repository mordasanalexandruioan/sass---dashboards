let closeAlert = document.getElementById('closeAlert');

closeAlert.addEventListener('click', eventCloseAlert);



let settingUl = document.querySelector('.settings-ul');

settingUl.addEventListener('click', eventSettings);